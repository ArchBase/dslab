#include <stdio.h>
#include <stdlib.h>

struct node{
    int data;
    struct node *next;
};

struct node start;

void insert(){
    struct node *newNode = (struct node*)malloc(sizeof(struct node));

}





